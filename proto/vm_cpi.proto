syntax = "proto3";
package org.solana.sealevel.v1;
import "invoke.proto";
import "context.proto";
import "vm.proto";

message CPISigner{
    // max count 16, max 32 bytes each
    repeated bytes seeds = 1;
}

message CPIInstr{
    bytes callee_program_id = 1;
    repeated InstrAcct acct_metas = 2;
    bytes data = 3;

    repeated AcctState accounts = 4;
    repeated CPISigner signers_seeds = 5;
}

message CPIContext {
  // The vm context
  VmContext vm_context = 1;
  InstrContext instr_context = 2;
}